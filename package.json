{
  "name": "@asana/command-mcp",
  "version": "0.2.0",
  "type": "module",
  "license": "MIT",
  "author": "Asana",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "asana-command-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "NOTICES.md",
    "SECURITY.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --platform node --target node22 --clean --sourcemap",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src tests scripts",
    "test": "vitest run --exclude 'tests/integration/**'",
    "test:integration": "vitest run tests/integration",
    "validate:live": "npm run build && tsup scripts/live-validation.ts --format esm --platform node --target node22 --out-dir scratch/live-validation --clean && node scratch/live-validation/live-validation.js",
    "check": "npm run typecheck && npm run lint && npm test && npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@napi-rs/keyring": "1.3.0",
    "asana": "3.2.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "@types/node": "26.1.1",
    "tsup": "8.5.0",
    "typescript": "5.9.2",
    "vitest": "3.2.7"
  }
}
